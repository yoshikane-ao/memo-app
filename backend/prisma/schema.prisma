// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Get a free hosted Postgres database in seconds: `npx create-db`

generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Memos {
  id        Int             @id @default(autoincrement())
  title     String          @db.VarChar(20)
  content   String
  createdAt DateTime        @default(now())
  updatedAt DateTime        @updatedAt
  histories MemoHistories[]
}

model MemoHistories {
  id        Int      @id @default(autoincrement())
  memoId    Int      @map("memo_id")
  title     String
  content   String? // ?をつけることでNULLを許容(TEXT)
  createdAt DateTime @default(now()) @map("created_at") // @db.Timestamptz を付ける場合もあります
  memo      Memos    @relation(fields: [memoId], references: [id], onDelete: Cascade)
}

// model Memos {
//   id        Int      @id @default(autoincrement())
//   title     String
//   content   String
//   tags      String[] // タグの配列
//   tab       String?  // タブ名 (オプション)
//   createdAt DateTime @default(now())
//   updatedAt DateTime @updatedAt
// }
