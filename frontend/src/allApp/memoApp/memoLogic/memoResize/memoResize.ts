// 機能だけを切り出したファイル
export const useMemoResize = (memoId: number) => {
    const saveSize = (width: number, height: number) => {
        // API呼び出しロジック
    };

    // ResizeObserverの設定など
    return { saveSize };
};